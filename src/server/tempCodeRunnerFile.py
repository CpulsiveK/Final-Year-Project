files = []
        # data_struct = RoutingInfoDataStructure()
        # peer, addr = s_sock.accept()
        # number_of_files = int(peer.recv(1024).decode())
        # file_name = peer.recv(1024).decode()
        # file_size = peer.recv(1024).decode()

        # for i in range(number_of_files):
        #     files.append(File(file_name, file_size)[i])